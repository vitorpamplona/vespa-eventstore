# The bundle is a Gradle build artifact — :container-store:containerBundle emits
# container-store/build/bundle/containerstore-bundle.jar (~44 MB of embedded jars).
# deploy.sh builds and deploys it; nothing here belongs in git.
